<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Panel Directora</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="p-8 bg-gray-50">
  <h1 class="text-2xl font-bold mb-6">📊 Reporte de Asistencias</h1>
  <table class="w-full bg-white rounded shadow">
    <tr class="bg-gray-200">
      <th class="p-2">Alumno</th>
      <th>Grupo</th>
      <th>Fecha</th>
      <th>Estado</th>
    </tr>
    <% asistencias.forEach(a => { %>
    <tr>
      <td class="p-2"><%= a.nombre %></td>
      <td><%= a.grupo %></td>
      <td><%= a.fecha %></td>
      <td><%= a.estado %></td>
    </tr>
    <% }) %>
  </table>
</body>
</html>